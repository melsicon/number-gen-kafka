apiVersion: apps/v1
kind: Deployment
metadata:
  name: "number-gen-kafka"
spec:
  selector:
    matchLabels:
      app: "number-gen-kafka"
  template:
    metadata:
      labels:
        app: "number-gen-kafka"
    spec:
      containers:
        - name: "number-gen-kafka"
          image: "docker.io/lehwwmcrnjnhzjaglr/number-gen-kafka"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "number-gen-kafka"
spec:
  selector:
    app: "number-gen-kafka"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080